Program
{
  Int x = -10;
  
  Int f(Int x, Int y) {
    if(x == y) then {
        return x;
    }
    
    if(x < y) then {
        return f(x + 1, y);
    }
    
    return f(x, y + 1);
    return f(f(x - -1, y * 2), y + 1);
  }
  
  Int inc (Int x ) {
      return x + 1;
  }
  
  Int get_int() extern;
  
  void print(Int i) extern;
  
  void main()
  {
	Int v   = 1%2;
	Int w   = 1/2;
	Bool x  = 1<2;
	Bool y  = 1>2;
	Bool z  = 1==2;

	Int i = 0;
	while(True) {
		if((i % 2) == 0) then { i = i - 1; }
		i = i + 1;
	}

      	if (0 < 1) then
        {
       		print(0);
        }
      	else 
        {
        	return print(inc(1));
        }
  }
}
